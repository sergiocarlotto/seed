namespace Seed.UnitTests;

// Placeholder unit test so the project builds and runs until the first
// domain/application module adds real tests (ADR-0003, ADR-0008).
public class ScaffoldSmokeTests
{
    [Fact]
    public void Scaffold_is_wired()
    {
        Assert.True(true);
    }
}
